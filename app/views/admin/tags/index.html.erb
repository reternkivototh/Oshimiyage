<h3>タグ一覧・追加</h3>

<%= form_with model: @tag, url: admin_tags_path do |f| %>
  <%= f.text_field :name %>
  <%= f.submit '新規登録' %>
<% end %>

<h5>タグ</h5>
<% @tags.each do |tag| %>
  <%= tag.name %>
  <%= link_to '編集する', edit_admin_tag_path(tag.id) %>
<% end %>
